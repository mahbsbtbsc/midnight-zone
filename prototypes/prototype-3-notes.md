# Prototype 3 — Silhouettes + OBIS Dataset

Added unique p5.js bezier silhouettes for all 12 species.
Added OBIS as a supplementary premade dataset.
OBIS region count now drives animation speed.
Added GBIF_FALLBACK lookup so counts never show 0.
Hover repulsion removed — replaced with highlight ring.

## What worked
- Each creature now visually distinct
- Dual data sources (GBIF API + OBIS dataset)
- Info card shows both GBIF and OBIS records

## What needed changing
- Still empty space in abyssal zone
- Only 12 species — not enough to fill full depth range
